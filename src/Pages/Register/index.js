import classNames from "classnames/bind";
import styles from './Register.module.scss'

const cx = classNames.bind(styles)

export default function Register(){
    return(
        <h1>DK vat dung ne</h1>
    )
}