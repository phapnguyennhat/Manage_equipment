import classNames from "classnames/bind";
import styles from './Listregister.module.scss'

const cx = classNames.bind(styles)

export default function Listregister(){
    return(
        <h1>Danh sach dang ky ne</h1>
    )
}